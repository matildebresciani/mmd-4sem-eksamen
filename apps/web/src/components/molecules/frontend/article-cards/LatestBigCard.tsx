import type { Article } from '@/payload-types';

type Props = {
    article: Article;
    className?: string;
};

const LatestBigCard = ({ article, className }: Props) => {
    return <></>;
};

export default LatestBigCard;
